// Loaded lazily by <LazyMotion> in __root.tsx so the animation engine is not
// on the critical path: the page renders first, animations attach after.
export { domAnimation as default } from 'motion/react'
